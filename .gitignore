# === macOS ===
.DS_Store

# === IDE / 编辑器 ===
.idea/
.vscode/
*.iml

# === Node / pnpm ===
node_modules/
dist/

# === 测试缓存 (Vite / Vitest) ===
.vite-cache/
.vitest-cache/

# === 日志 ===
*.log
npm-debug.log*

# === Java / Maven ===
target/
*.class
*.jar
*.war

# === 备份 / 临时文件 ===
.src.backup-*/
*.bak
*.tmp

# === 环境变量 ===
.env
.env.local

# === Claude harness ===
# 本段采用白名单模式：默认忽略 .claude/ 下全部，只放行框架必需的目录和文件。
# 放行：agents/（子代理定义）、roles/（角色说明）、settings.json（项目级配置）
# 显式忽略：worktrees/（运行时隔离副本）、settings.local.json（本地覆盖）、.DS_Store
.claude/*
!.claude/agents/
!.claude/roles/
!.claude/settings.json
.claude/worktrees/
.claude/settings.local.json
.claude/.DS_Store

# === Codex 调用日志 ===
# codex.sh 每次调用写入的行式日志（含 stdin/stdout 全文，体积大且可能含任务上下文，不入仓）
harness/trace/codex/
harness/.cache/

# === 个人笔记 ===
待办.md